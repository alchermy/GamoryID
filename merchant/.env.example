# Backend API base (Sanctum cookie auth) — no trailing slash
VITE_API_URL=http://localhost:8000/api/v1

# Marketing site (public-web) — used for the Terms / Privacy links on register
VITE_PUBLIC_WEB_URL=http://localhost:5174
