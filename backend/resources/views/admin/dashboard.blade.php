<!doctype html><html lang="th"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>GamoryID Admin</title>
<style>body{font:14px Tahoma,sans-serif;background:#f4f7fb;color:#14253d;margin:0}header{background:#07172e;color:#fff;padding:16px 5vw;display:flex;justify-content:space-between}main{max-width:1180px;margin:auto;padding:28px 20px}.stats{display:grid;grid-template-columns:repeat(3,1fr);gap:14px}.card{background:#fff;border:1px solid #dbe4ee;border-radius:12px;padding:18px;margin-bottom:18px}.num{font-size:30px;font-weight:800;color:#0879d9}table{width:100%;border-collapse:collapse}th,td{text-align:left;padding:12px 8px;border-bottom:1px solid #e7edf4}select,input{padding:8px;border:1px solid #cbd7e4;border-radius:6px}button{padding:8px 12px;border:0;border-radius:6px;background:#0879d9;color:#fff}.success{background:#e7f8ef;padding:10px;border-radius:8px}@media(max-width:700px){.stats{grid-template-columns:1fr}.card{overflow:auto}}</style></head><body><header><strong>GamoryID / Super Admin</strong><form method="post" action="{{ route('admin.logout') }}">@csrf<button>ออกจากระบบ</button></form></header><main>@if(session('message'))<p class="success">{{ session('message') }}</p>@endif<div class="stats"><div class="card"><div>ร้านทั้งหมด</div><div class="num">{{ $totals['shops'] }}</div></div><div class="card"><div>รายการไอดี</div><div class="num">{{ number_format($totals['items']) }}</div></div><div class="card"><div>ผู้ใช้งาน</div><div class="num">{{ $totals['users'] }}</div></div></div>
<section class="card"><h2>ร้านค้า</h2><table><thead><tr><th>ร้าน</th><th>สถานะ</th><th>สต็อก</th><th>จัดการ</th></tr></thead><tbody>@foreach($shops as $shop)<tr><td>{{ $shop->name }}</td><td>{{ $shop->status }}</td><td>{{ number_format($shop->inventory_items_count) }}</td><td><form method="post" action="{{ route('admin.shops.update',$shop) }}">@csrf @method('PATCH')<select name="status">@foreach(['trialing','active','grace_read_only','suspended','cancelled'] as $status)<option @selected($shop->status===$status)>{{ $status }}</option>@endforeach</select><button>บันทึก</button></form></td></tr>@endforeach</tbody></table></section>
<section class="card"><h2>แพ็กเกจ</h2>@foreach($plans as $plan)<form method="post" action="{{ route('admin.plans.update',$plan) }}" style="display:flex;gap:8px;flex-wrap:wrap;margin-bottom:10px">@csrf @method('PATCH')<input name="name" value="{{ $plan->name }}"><input name="active_inventory_limit" type="number" value="{{ $plan->active_inventory_limit }}"><input name="member_limit" type="number" value="{{ $plan->member_limit }}"><input name="price_thb" type="number" value="{{ $plan->price_thb }}"><button>บันทึก</button></form>@endforeach</section>
<section class="card"><h2>รอตรวจสลิป ({{ $pendingPayments->count() }})</h2>@forelse($pendingPayments as $payment)<p>{{ $payment->shop->name }} · {{ $payment->plan->name }} · ฿{{ number_format($payment->expected_amount,2) }} — {{ $payment->review_note }}</p>@empty<p>ไม่มีรายการรอตรวจ</p>@endforelse</section></main></body></html>
